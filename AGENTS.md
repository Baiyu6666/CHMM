# Test Instruction

- 我不在意对老版的兼容性，每当你搞新版本的时候，不需要专门兼容老的。你就自然地把老的也改了。比如我吧某个参数名从“obs—avoid”改成“obstacle”，你不需要保留对老名字的兼容，你就把各种地方的老名字换成新名字即可。除非我明确告诉你保留兼容
- 为了节约时间，改代码的时候不要同步更新各种md文件，比如readme等等，否则会产生频繁改动。只有我最后让你调整md的时候你在统一调整
- 

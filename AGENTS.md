# Test Instruction

- 我不在意对老版的兼容性，每当你搞新版本的时候，不需要专门兼容老的。你就自然地把老的也改了。比如我吧某个参数名从“obs—avoid”改成“obstacle”，你不需要保留对老名字的兼容，你就把各种地方的老名字换成新名字即可。除非我明确告诉你保留兼容
- 为了节约时间，改代码的时候不要同步更新各种md文件，比如readme等等，否则会产生频繁改动。只有我最后让你调整md的时候你在统一调整
- 默认使用项目python interpeter 也就是conda segment。要装新的包或者运行代码都用这个环境
- 显示公式和表格的时候都是用unicode
- 给我run 实验相关命令的时候默认使用run_benchmark  而不是run one。使用我的方法，MAP，SWCL的时候始终只使用一个method seed，不需要重复不同seed

# 效率与验证策略

- “轻量化”只减少重复搜索、重复读取、重复构建和工具调用，不减少必要的代码理解、review、安全检查或测试。
- 已知目标文件和位置时直接读取、修改，不要为了形式再次全仓库搜索；只有接口变化、依赖关系不明确或需要确认调用方时才做代码搜索。
- 能在一次调用中完成或并行完成的独立搜索、读取和检查应合并，避免串行的小工具调用。
- 文档任务默认只读取相关文档、一次性修改并执行一次格式或 `git diff --check` 检查；不要运行 Docker、catkin 或无关代码测试。
- 普通代码修改完成后再进行一次针对性测试和一次最终构建。不要因纯文案、注释、日志字段或不影响构建的微小变化反复完整构建 Docker/catkin。
- 只有构建或测试发现真实问题、或者后续代码改动使已有验证失效时，才进入下一轮构建和测试。
- 当前 session 中已经通过、且相关代码和依赖没有变化的测试不要重复运行；复用已有验证结果。
- 已有日志、历史运行数据或当前 session 的结果足以回答问题时，优先分析这些证据，不要为了“再确认一下”重新运行完整实验。
- 运行 Docker、catkin、实验或其他耗时命令前，先区分“本次修改必须验证”和“可选的额外验证”。必须验证的操作可以直接执行，但应先说明原因和预计耗时；预计超过 30 秒的可选验证必须先说明价值，未经用户确认不要执行。
- 项目已有启动、构建或实验脚本以及 GUI 入口时，优先使用这些标准入口。不要在未检查现有脚本和 Compose 配置的情况下手写等价命令，因为标准入口可能负责 UID、环境变量、网络和安全前置条件。
- 对 `robot/stage_cons_iiwa14` 执行 Docker 构建时，必须通过 GUI 或项目脚本构建；如确需直接运行 Compose，先用 `docker compose config` 确认 `USER_UID` 等于宿主机 `id -u`。不得依赖 UID 1000 的默认值。
- 耗时构建失败时，先检查命令、工作目录、环境变量、挂载权限和缓存失效原因；不要在未定位原因前直接重复完整构建。由错误命令导致的失败应先修正调用方式，再进行唯一一次必要的重试。
- 修改完成后的状态、日志、diff 和服务检查应尽量合并为一次最终检查，不要连续重复查询同一状态。
- 排查过程中发现的旁支问题默认只记录并告知用户，不要自动扩展成额外改造；只有它直接阻塞当前需求、影响数据正确性或涉及机器人安全时才纳入当前修改。
- 按风险选择验证强度：小改动做语法/格式检查；普通逻辑改动做针对性测试和最终构建；公共接口、ROS topic/service/action contract、IK、时间参数化、状态机、C++ driver、controller、watchdog 和线程同步等改动必须检查调用方并做相应构建或离线集成测试。
- 机器人控制和安全路径不得为了速度跳过必要验证。必须继续检查 position-command gate、heartbeat、abort、FRI 状态、限位、轨迹连续性和失效回退；离线测试不得描述为真机验证。
